<div class="col-md-10 ">
  <h1>
    <%= @job.title %></h1>
  <p>
    <%= simple_format(@job.description) %>
  </p>

  <div class="text-center">
    <%= link_to("投交覆歷", new_job_resume_path(@job), :style => "font-size:18px; text-decoration:underline; ") %>
  </div>
</div>
